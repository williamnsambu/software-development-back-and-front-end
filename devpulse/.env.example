# -------------------------
# Database
# -------------------------
ConnectionStrings__db=Host=localhost;Database=devpulse;Username=devpulse;Password=super-secret

# -------------------------
# Auth
# -------------------------
Auth__SigningKey=replace-with-long-random-signing-key

# -------------------------
# GitHub OAuth + Webhook
# -------------------------
GitHub__ClientId=your-github-oauth-client-id
GitHub__ClientSecret=your-github-oauth-client-secret
GitHub__WebhookSecret=your-github-webhook-secret

# -------------------------
# Jira Integration
# -------------------------
# Jira base URL stays in appsettings.json (non-secret)
# User provides Jira email + API token at runtime (so not committed)

# -------------------------
# OpenWeather
# -------------------------
OpenWeather__ApiKey=your-openweather-api-key

# -------------------------
# Slack (optional integration)
# -------------------------
Slack__ClientId=your-slack-client-id
Slack__ClientSecret=your-slack-client-secret
Slack__BotToken=your-slack-bot-token

# -------------------------
# Infisical / Vault (for local dev or CI)
# -------------------------
# Only needed if we want to use Infisical CLI or Vault client
INFISICAL_TOKEN=your-infisical-token
INFISICAL_PROJECT_ID=your-infisical-project-id

# For Vault OSS dev mode
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=root